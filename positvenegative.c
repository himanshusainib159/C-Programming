#include <stdio.h>
int main()
{
    //check if number is +ve or -ve 
    int num;
    scanf("%d",&num);
    if(num>=0)
    {
        printf("positive number ");
    }
    else
    printf("negative number");
    return 0;
}